% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stattest.R
\name{stattest}
\alias{stattest}
\title{Perform Statistical Significance Testing}
\usage{
stattest(
  data,
  by,
  var,
  group1,
  answer,
  wt = NULL,
  group2 = NULL,
  answer2 = NULL,
  var2 = NULL,
  data2 = NULL,
  wt2 = NULL,
  crit = 1.96,
  type = "auto"
)
}
\arguments{
\item{data}{Data frame. Primary survey data.}

\item{by}{Character. Column name of the grouping variable.}

\item{var}{Character. Column name of the variable to analyze in \code{data}.}

\item{group1}{Character. Name of the first group level.}

\item{answer}{Character. Response value of interest for \code{var}.}

\item{wt}{Character, optional. Column name for survey weights.}

\item{group2}{Character, optional. Name of second group level (for two-group tests).}

\item{answer2}{Character, optional. Response value for \code{var2} (for one-group, two-question test).}

\item{var2}{Character, optional. Second question variable (for one-group, two-question test).}

\item{data2}{Data frame, optional. Second survey data (for independent surveys).}

\item{wt2}{Character, optional. Weight column for \code{data2}.}

\item{crit}{Numeric. Critical value for significance (default 1.96 ~ 95\% CI).}

\item{type}{Character. Type of test: \code{"auto"} (default), \code{"horse_race"}, \code{"two_groups"}, \code{"one_group_two_questions"}.}
}
\value{
A data frame summarizing:
\itemize{
\item Proportions (P1/P2)
\item Sample sizes
\item Weight sums, squared weights, design effects
\item Differences, tolerances, and significance
\item Test type
}
}
\description{
Computes differences in proportions for survey data, supporting:
\enumerate{
\item Single survey with one question (horse race)
\item Single survey with two groups
\item Single survey with one group, two questions
\item Two independent surveys
}
}
\details{
Also calculates standard errors, margin of error, design effect (if weights are provided),
and flags whether differences are statistically significant.
\itemize{
\item Uses \code{pewmethods::get_totals()} internally.
\item For weighted surveys, calculates design effects and adjusts standard errors.
\item If \code{type = "auto"}, the function attempts to infer test type based on supplied arguments.
\item Returns a tidy table that can be exported to Excel.
}
}
\examples{
\dontrun{
stattest(
  data = survey1,
  by = "gender",
  var = "support_candidate",
  group1 = "Male",
  group2 = "Female",
  answer = "Yes",
  wt = "weight"
)

# Two independent surveys
stattest(
  data = survey1,
  data2 = survey2,
  var = "support_candidate",
  var2 = "support_candidate",
  by = "gender",
  group1 = "Male",
  group2 = "Female",
  answer = "Yes",
  answer2 = "Yes",
  wt = "weight",
  wt2 = "weight"
)
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freq.R
\name{freq}
\alias{freq}
\title{Frequency Table with Optional Weighting}
\usage{
freq(data, var, weight = NULL, include_na = FALSE, digits = 0)
}
\arguments{
\item{data}{A data frame.}

\item{var}{A character string giving the name of the variable to tabulate.}

\item{weight}{Optional character string giving the name of the weight variable.
If \code{NULL} (default), unweighted counts are used.}

\item{include_na}{Logical. If \code{TRUE}, NA values are included as a \code{"(Missing)"}
row. Default is \code{FALSE}.}

\item{digits}{Integer. Number of decimal places for percentages. Default is \code{0}.}
}
\value{
A data frame with columns for the variable values, percentage
(\code{weighted_pct} or \code{unweighted_pct}), and \code{unweighted_n}. The final row
shows the total percentage and total unweighted N.
}
\description{
\code{freq()} produces a frequency table for a single variable, with optional
survey weighting. Returns a data frame with percentages and unweighted counts,
with a Total row at the bottom. Factor level ordering is preserved. NA values
are either excluded (default) or shown as "(Missing)".

The percentage column is labeled \code{weighted_pct} when a weight is supplied and
\code{unweighted_pct} when not. Counts are always unweighted.
}
\examples{
\dontrun{
# Basic unweighted frequency
freq(data, "party3")

# Weighted frequency
freq(data, "party3", weight = "WEIGHT_PID_ADJ")

# Weighted frequency including NAs
freq(data, "MAGA1", weight = "WEIGHT_PID_ADJ", include_na = TRUE)

# Weighted frequency with decimal places
freq(data, "party3", weight = "WEIGHT_PID_ADJ", digits = 1)

# On a combo variable
freq(data, "party3MAGA1_combo", weight = "WEIGHT_PID_ADJ")

# On a total variable
freq(data, "rxmany_Total", weight = "WEIGHT_PID_ADJ")
}
}

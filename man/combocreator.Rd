% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combocreator.R
\name{combocreator}
\alias{combocreator}
\title{Combine a Parent and Child Factor Variable}
\usage{
combocreator(data, var1, var2, child_asked_values = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{var1}{The parent factor variable.}

\item{var2}{The child factor variable (asked of a subset of \code{var1} levels).}

\item{child_asked_values}{Optional character vector of \code{var1} levels for which
\code{var2} was asked. If \code{NULL} (default), these are auto-detected from the data.}
}
\value{
The original data frame with a new factor variable named
\verb{\{var1\}\{var2\}_combo} inserted immediately after \code{var2}.
}
\description{
\code{combocreator()} combines a parent factor variable and a child factor variable
that was only asked of a subset of respondents into a single combo variable.
For respondents who were asked the child question, the combo variable takes the
form \code{"[parent label] - [child label]"}. For respondents who were not asked the
child question, the combo variable retains the parent label. The new variable is
placed immediately after the child variable in the dataset.

The function auto-detects which parent groups were asked the child question by
finding parent levels with any non-NA child responses. This can be overridden
by supplying \code{child_asked_values} manually.

Level ordering follows the original factor ordering of \code{var1} and \code{var2} rather
than alphabetical sorting.
}
\examples{
\dontrun{
# Auto-detect which groups were asked the child question
data <- combocreator(data, party3, MAGA1)

# Manually specify which groups were asked
data <- combocreator(data, party3, maga1,
  child_asked_values = c("Republican", "Republican-leaning Independent"))
}
}
